import Button from "./Button"

const Header = () => {
    return (
        <header className="header">
        <h1>Task Manager</h1>
        <Button color="black" text="Add"/>
        </header>
    )
}

export default Header
